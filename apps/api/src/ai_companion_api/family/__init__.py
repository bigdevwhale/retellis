"""Empty package marker — the family module is split across:
- ``family/store.py`` (FamilyStore Protocol + impls)
- ``family/attach.py`` (auto-attach helpers called from auth flows)
- ``routers/family.py`` (HTTP surface)
"""
